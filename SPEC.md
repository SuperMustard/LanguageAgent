# SPEC — 语言口语演练 Agent

## 一句话定位

一个整合的语言学习工作流 agent。核心负责**文字聊天做不到的活学场景**（实时应变、发音、语速），
并把两处素材——① 口语演练中萃取的病句/生词/话术，② 精听真实材料时"没听懂的地方"提炼出的
collocation/生词——统一导出成 Anki 插件 **langhelper** 吃得进的格式，由 Anki 侧负责抗遗忘和文字诱导。

**自造边界（重要）**：这是一个整合工作流，不是各自为政的小软件；但只自造别人做不好/做不了的中间
那段（提炼、诱导、演练、Debrief，及把它们串成一个流），**重活外包给成熟工具**——抗遗忘外包给
Anki，播放/取字幕外包给成熟播放器。两头用标准接口对接：播放器 → agent 的接口是 **SRT**（可替换，
不绑定具体播放器）；agent → Anki 的接口是 **langhelper 导入格式**。中间 agent 这一大块（四模块 +
同一个 SQLite）是完全掌控、紧密整合的核心。**注意边界划在"提炼进 agent、播放留在外面"这条线上**：
收进 agent 的是提炼（SRT→素材→表），不是播放器本身。

分工原则：**Agent 负责口语活学演练 + 精听提炼 → langhelper 负责文字诱导复习 → Anki 负责抗遗忘。**

---

## 使用者背景

- 母语中文，练习目标语言：**英语 (en)** 和 **法语 (fr)**
- 典型场景：法语诊所应对情绪不佳的客人、英语工作面试等
- 法语需要细腻表达：vouvoiement（敬语）、诊所同理心话术

---

## 模块（收敛后）

文字侧诱导/造句整块交给 langhelper，抗遗忘交给 Anki，播放/取字幕外包给成熟播放器。agent 自身
收敛为**四件事**：① 真实情境模拟与对话，② 对话总结与反馈诊断（含 2.5 话术推荐），③ 记忆与诱导，
④ 精听提炼（Intensive-Listening Mining）。其中 ④ 是把原先设想的"独立输入侧管线"收进 agent 后的
第四模块——因为目标是**整合工作流**而非分开的小软件，提炼作为 agent 内部模块、用 agent 自己的
SQLite，而不是一个游离在外、只靠文件跟 agent 打交道的独立脚本。

### 1. 真实情境模拟与对话（Simulation Mode）— agent 独有核心

- 多场景角色扮演：按给定情境精准扮演角色，保持真实人设多轮对话
- 语音对讲：STT/TTS，用口语对答，训练发音、语速、应变
- **全双工实时对话**（用 Pipecat 编排）：VAD 自动判断"说完了"，不用手动点录音按钮；
  支持打断（学习者插话时 AI 的语音回复可以被切断）——更接近真实对话的应变训练，
  也更沉浸（演练时不用分心操作 UI）
- **铁律：演练中 AI 绝不跳出角色纠错。** 纠错只在 Debrief 阶段发生。
  这条铁律跟全双工与否无关——全双工只改变"怎么把话传进传出"，不改变"演练时绝不纠错"

### 2. 对话总结与反馈诊断（Debrief & Feedback）— agent 核心

- 演练结束（显式信号）后，单独一次调用分析整场表现
- 产出病句四要素 + 中文题面（见 Debrief Schema）
- 产出需要记忆的生词
- **产出专业应对话术推荐**（见模块 2.5）——同一次 Debrief 调用一并产出，不额外多调一次
- **纠错部分纯文字**，不走语音

### 2.5 专业应对话术推荐（Professional Phrase Recommendation）— agent 核心

这是学习素材的**第三类**（并列于病句、生词），也是使用者最看重的能力：**用专业方式
处理难缠客户**（诊所/按摩治疗环境里情绪化、不满、不配合的客人）。

- **时机**：演练后，并入 Debrief 那一次调用产出（不新增调用）
- **不是散装客套话，而是有骨架的专业应对**。推荐按专业维度组织：
  - `同理承接`（先接住情绪，不急着辩解/解决）
  - `设立边界`（专业地坚持原则/流程/安全，不失礼也不一味顺从）
  - `降级冲突`（对方升温时往下缓和，而非对着来）
  - `重定向解决`（情绪接住后把对话引回"我们能怎么帮你"）
  - 法语额外维度：`vouvoiement 敬语`在冲突降级里的得体运用
- **锚定 transcript**：优先针对学习者在这场演练里**实际遇到的刁难、应对得不够专业**的地方，
  给更专业的说法——而不是凭空罗列场景话术。这也是为什么放在演练后：有真实应对可复盘。
- **防重复**：产出前把**已在库的话术清单**传进 prompt，要求避开（语义去重）；数据库层面
  另有字面去重（见下）。已掌握/最近推过的不再推。
- **进入现有诱导+掌握度循环**：推荐的话术存进新表 `pro_phrases`，初始 mastery=0，之后由
  `induction.py` 当隐藏目标注入未来演练，引导学习者自然用出专业应对；用对了 +1、用错 -1、
  掌握了不再推——**"防止重复推荐"因此几乎白送**，不必另写判重逻辑。
- **导出：已定义契约，走「表达块卡」。** 见"导出格式契约 → 表达块卡"。langhelper 早先只支持
  病句卡/单词卡，专业话术是整句、塞进单词卡会错配（单词卡的例句/翻译诱导玩法对整句话术不合适），
  当时因此暂缓。现在新增第三种卡型**「表达块卡」**专收整块搭配/整句表达，话术导出的错配前提消失，
  接一根导出管道即可。`pro_phrases` 表字段（`phrase` / `meaning` / `usage_note` / `language`）
  早已**为导出预留**，正好兑现"不用回头给老数据补字段"。
- **抗遗忘由单轨变双轨**：话术原先只靠 agent 口语诱导（在真实对话语境里被引导用出来）——那是被
  langhelper 不支持话术卡**逼出来的妥协**，非主动选择（字段都预留了，本就想导出）。现在话术走
  **agent 口语诱导 + langhelper 文字刷卡**双轨，与生词/病句一致。两个 mastery（口语侧 agent 管、
  文字侧 Anki 管）沿用"一期不双向同步"原则，无需改动；只需知道话术现在也有**文字侧 mastery**，
  别把两个掌握度搞混。口语侧诱导仍是 langhelper 给不了的能力（文字插件无法在你张嘴说法语时埋钩子），
  双轨是互补而非替代。

### 3. 记忆与诱导（Memory & Induction）

- **文字侧诱导：整块交给 langhelper。** 不在 agent 里实现造句/翻译诱导
  - langhelper 的单词卡高阶 = 诱导你用目标词翻译
  - langhelper 的病句卡高阶 = 生成同类错误句针对练习
  - langhelper 的表达块卡高阶 = 语境召唤（情境应对召唤 / 搭配填空，见导出契约）
- **口语侧诱导（agent 独有，二期）：✅ 已实现（2026-08）** 新语音场景开始前，从 agent
  自己的 SQLite 检索 1~2 条旧表达，作为**隐藏目标**注入角色卡，让 AI 设计语境引导使用。
  见 `langpractice/induction.py` + `voice_bot.py` 的 `run_bot()`。
  - 检索条件：`语言 + 掌握度 + 最后练习时间`（按 mastery 升序、last_practiced 升序取前
    N 条）
  - **不限定场景**：只按语言检索，跟当前选的场景无关
  - **诱导素材有三个来源**：`expressions`（病句）、`words`（生词）、`pro_phrases`（专业话术，
    新增）。三类混合时用**保底 + 上限**配比（config，见"配置项"）：保证专业话术有配额、
    不被生词淹没，因为它是使用者最想练的。别让某一类挤掉其它类。
  - **collocation 现为第四个诱导来源（收进 agent 后）**：精听提炼（模块 4）产出的 collocation
    进 agent 自己的表，因此 `induction.py` 天然可检索到，不再是"外部管线跨库注入"的问题。口语
    激活走**两条独立通道**：
    - **今日通道（一期做）**：当天精听提炼刚进库、mastery=0 的 collocation，在当天那场对话里作为
      高优先级隐藏目标注入（方案 A：oral summary 与 collocation 激活叠在同一场对话）。因为它们
      mastery=0、last_practiced 最早，按现有"最久没碰"检索规则天然排前，**基本不用改检索逻辑**。
    - **历史通道（常规）**：往期 collocation 跟病句/生词/话术一起，按**固定每场配比**被采样诱导。
  - **采样激活原则（重要）**：口语诱导是**采样激活层，不负责覆盖**。抗遗忘的完整覆盖由 Anki 保证
    （所有病句/生词/collocation/话术都导出、都会被刷到）。口语诱导只做 Anki 给不了的事——在真实
    开口语境里激活一部分表达。因此单场用**固定每场配比**从各来源采样，不追求覆盖全部；某场没被
    诱导到的条目不会遗忘（Anki 兜底），下次采样有机会即可。这条原则一刀切掉"某类要不要全包进
    诱导"的纠结：口语侧只管激活质量，覆盖交给 Anki。
  - **固定配比取代旧的"淹没保护 + 阈值"方案**：不设"库涨到多少才加保护"的限时窗口。单场
    `INDUCTION_MAX_TARGETS` 封顶、各来源保底（`pro_phrases` 保底优先保护）——collocation 涨到
    多少都只在自己名额内竞争，永远淹不到 pro_phrases。库空阶段某来源没货时，名额让给别的来源
    顶上（能注满就注满，别浪费单场机会），有货后自然回到配比。冷启动可跑、涨上去不淹，一次写死
    不用回来调。
  - **掌握度更新算法：✅ 已实现（2026-08）**。演练结束时除了 Debrief，还有一次独立的
    非流式复盘调用（`induction.review_induction_usage()`，prompt 见
    `prompts/induction_review_prompt.md`），判断这场诱导目标有没有被用上：
    讲对了 mastery +1，讲错了 mastery -1（下限 0），没用上不动（保持"最久没碰"排前面，
    下次继续诱导）。用真实 Groq API 验证过：正确区分"用对了"和"完全没用到"两种情况。
  - 这是 langhelper 永远给不了的能力（文字插件无法在你张嘴说法语时埋钩子）

### 4. 精听提炼（Intensive-Listening Mining）— agent 模块（收进后新增）

把真实材料的精听变成学习素材的采集口，**核心是"以你没听懂的地方为中心"**，而非漫无目的从字幕
里挑词。对应 roadmap B2→C1 的 authentic content + collocations。

- **输入：Trancy 导出的两种 CSV，走两条入口**（外包给 Trancy 浏览器扩展做双字幕/收藏/取字幕）。
  Trancy 的收藏是**词 / 句两级**，导出成两个结构不同的 CSV，语义和去向都不同，别混成一条：
  - **词表 CSV**（`Word / Phonetic / Translation / Date`）：你**点词收藏**的 = 你不认识这个词 =
    明确的生词。**这条几乎不用干预、也不经 Groq**——"点词"这个动作本身已完成分类（不认识才点）。
    Trancy 连中文义带音标都给全了：`Word`→词、`Translation`→中文义、`Phonetic`→音标（白送，
    可存进 `words` 备用）。直接进 `words` 表 → 生词卡。⚠️ `Translation` 字段偏脏（形如
    `n. 独处 独居; web. 孤独 寂寞…`，按 `;` 分段、每段 `词性. 释义…`，末尾常有 `web.` 网络释义
    段，含剧名等噪声如"白夜追凶"）——**一期入库前清洗成简洁词义**：① 丢掉 `web.` 段（网络释义
    质量最差）；② 保留词典词性段，每词性只取前 1–2 个主释义；③ 去掉尾部多余空格。例：
    `solitude` → `独处 独居`；`excess` → `n. 过度 超过; adj. 超额的`。纯字符串处理，不需模型
    （见"词表清洗规则"）。
  - **句表 CSV**（`Sentence / Translation / URL / Date`）：你**整句收藏**的 = 这句里有我没跟上的
    东西（可能是语块、可能是语音，需分诊）。**这才是走完整流水线的入口**（遍历标语音 → Groq 分
    生词/语块 → 入库）。注意句表**带 `URL`**（词表没有），语音那类靠它回原视频重听——正是
    `phonetic_notes.source` 的来源。
  - 素材来源分工：在线内容（YouTube / Netflix / Coursera 等）走 Trancy；本地文件 / 无字幕
    podcast 若 Trancy 够不着，退回 faster-whisper 命令行生成字幕。**采集工具本身不进 agent**，
    只有提炼进 agent。
  - **输入接口从 SRT 改为 CSV（契约变更）**：原设想接口是 SRT，实际落地改成 Trancy CSV。理由：
    ① Trancy 已跑通（在线素材双字幕、词/句两级收藏、CSV 导出都验过），换独立播放器不划算；
    ② CSV 比 SRT 对 agent 更好解析（结构化列 vs 自己写 SRT 三行块解析器）；③ 边界契约原文
    就写了"接口是 SRT（**可替换、不绑定具体播放器**）"，换成 CSV 正在预留的弹性范围内。
- **"没听懂"分三类，病根不同、去向不同**（这是模块 4 的核心分诊，别压成"从字幕挑词"）：
  - **生词**：不认识这个词（病根＝词汇量缺口）→ `words` 表 → 生词卡。
  - **语块 / collocation**：词都认识但不知道这么组合、这么用（病根＝用法/地道度缺口，roadmap
    B2→C1 的主攻对象）→ `collocations` 表 → 表达块卡。
  - **语音现象**：认识但没听出来的连读/弱读/吞音（病根＝**听觉解码能力，不是知识缺口**）。
    这一类**不进导出管道、不刷 Anki**——因为你不缺记忆，缺的是耳朵，给已认识的词做记忆卡是
    错配（同"话术塞单词卡会错配"）。只轻量留痕进 `phonetic_notes`（见下），供未来自我认知/
    可选跟读，不做抗遗忘。
- **两刀切：外层由你切，里层给模型切**（关键，别让模型判断它够不到的东西）：
  - **外层（语音 vs 语言）＝你自己标**。"认识但没听出来"这个判断的信息**只存在于你听的当下**
    （那个"这词我明明认识"的瞬间），Trancy CSV 里没有、模型从文本也推不出——同一句缩读，对你
    是"没听出来"、对别人可能是"不认识"，病根在你的感知里不在字幕里。故这一刀模型判不了，必须你标。
  - **里层（生词 vs 语块）＝Groq 切**。这是纯语言分析（词罕不罕见、几个词是不是固定搭配），
    模型够得到。用**已搭好的 Groq 客户端**（复用现成投资，见 CLAUDE.md 踩坑；分类是粗判断、
    快且量大，正是 Groq 主场）。⚠️ **提炼质量待验**：生成搭配释义/近义辨析这类吃"语言细腻度"
    的活，Groq 托管的开放权重模型可能不如 Haiku。**先全用 Groq 试跑、看真实卡片质量**：够用
    （自用标准可务实）就全 Groq、一个模型省事；搭配辨析总差意思，再**只把"提炼"这一步局部
    升级到 Haiku**，分类仍留 Groq。看输出再定，不预先拍板。
- **采集与处理分离，批量为主、单条为辅**（"批量 vs 即时"在架构上是同一流程的两种触发，不写两套）：
  - **采集**：Trancy 一键收藏，**即时、不打断听、不分类**——进"未处理"池。无脑存符合精听第一
    性原理（先整体盲听、逼耳朵先扛一遍，别听一句停一句毁掉沉浸）。
  - **处理（句表完整流水线，一次做完）**：你主动触发（默认听完一集后**批量**），加个"精听待处理"页
    （复用现有 web 前端模式）：
    - **第 0 步 导入+去重**：读句表 CSV，每行进未处理池；用 `Sentence` 字面跟已入库比一遍，避免
      重复处理同一句。
    - **第 1 步 你遍历、三选一（唯一人工判断）**：逐句展示 `Sentence`+`Translation`（可点 `URL`
      回去重听）。每句选一：
      - 「语音」= 我其实认识、就是没听出来 → **不进 Groq**，直接进 `phonetic_notes`。**顺手标一下
        "哪里没听清"**（句中哪个词/哪一小段，存 `word_or_span`）——这个你能可靠指出；**但不标
        "原因/现象类型"**（连读/弱读/吞音需语音学判断，你自己也常分不清，硬填是噪声非数据；
        留给未来由模型看 `sentence`+`word_or_span` 事后推断）。
      - 「语言」= 有不熟的词或搭配 → 交 Groq（第 2 步）。
      - 「跳过」= 误收藏/已掌握/没啥可学 → 标记已处理，不入任何表（整句收藏难免有噪声，得有出口，
        别逼每句硬塞进某类）。
    - **第 2 步 Groq 分诊+提炼（只跑第 1 步标「语言」的）**：一次调用可批量多句，做两件事——
      分诊（这句里值得学的是生词还是语块，允许一句产出多条：可能既有生词又有语块）+ 提炼
      （生词→规范词+中文义；语块→地道搭配+中文义+语境 note）。用已搭好的 Groq 客户端。
    - **第 3 步 入库分流**：生词→`words`；语块→`collocations`（mastery=0，进今日通道）；
      第 1 步语音句→`phonetic_notes`。三张表一次写完。
  - **即时**只是处理页里"这一条现在就处理"的快捷入口，走同一套后端逻辑，非独立流程。
  - **词表走另一条极简入口**：词表 CSV 直接入 `words`（点词=生词，Trancy 字段齐全），不经遍历、
    不经 Groq、不需语音判断。只有句表才跑上面的完整流水线。
- **产出**：语块 → `collocations` 表（初始 mastery=0）→ 表达块卡；生词 → `words` 表 → 生词卡；
  语音现象 → `phonetic_notes` 表（只存不导）。前两类导出 + 进口语诱导，第三类都不进。
- **去重**：在 agent 的 SQLite 内做（同库，字面为主），与 pro_phrases 等其它来源可互相看到；
  langhelper 导入时的字面去重作为跨工具最后一道防线。`phonetic_notes` 不参与导出去重。
- **与诱导的衔接**：见模块 3——当天新进、mastery=0 的 collocation 走"今日通道"在当天对话里被
  优先激活（方案 A），往期的走固定配比常规采样。（语音现象不进诱导。）
- **触发时机**：手动、沉浸、一次一篇（精听是主动学习场所，不是后台批处理苦力活，不需要自动化
  学习步骤本身；真要自动化的只是"抓取新材料+字幕"这个准备步骤）。

#### 精听分诊+提炼 Prompt（已用真实 Groq API 验证，2026-09-02）

对应第 2 步"Groq 分诊+提炼"。一次调用可批量传入多句（句子按编号拼进 `{{sentences}}`），
输出按 `index` 对应回原句。验证记录见上方"落地顺序"那条——两轮测试后质量达到自用标准，
建流水线时直接复用这份 prompt，不用重新设计：

```
你在做语言学习素材的精听提炼分诊（英语，母语中文的学习者）。下面是若干句子，每句都是学习者
精听时"整句收藏"的——即这句里有他没跟上的东西（词或搭配）。对每一句判断里面有没有：

1. 生词：学习者大概率不认识的词（不要挑基础常见词，比如 the/door/open/room 这类不算）。
2. 语块 / collocation：几个词组合起来的地道搭配或固定用法（词都认识但组合方式/用法未必知道）。
   只挑通用、可迁移的语块（换个句子还能用得上的那种），不要把这句话里临时的、
   句子特有的描述性短语（比如某个具体名词的修饰语）当成语块硬拎出来。

有些句子可能什么值得学的都没有（全是基础词、无地道搭配）——这种直接返回空数组，不要硬凑。

两条硬性要求：
1. 语块要写成词典原形，不要照抄句子里的屈折形式。动词用原形/动词短语原形（如句子里是
   "curled up"，输出 "curl up"；句子里是 "put it off"，输出 "put off"，代词占位不要带进来；
   句子里是 "emphasized"，输出 "emphasize the importance of"）。句子特有的信息（时态、代词、
   具体宾语）放进 note 字段说明语境，phrase 字段本身要是"换个句子还能直接用"的可迁移形式。
2. 逐个动词短语/固定搭配检查一遍再收尾，尤其一句话有多个独立语块时，不要找到一两个就停——
   把句子从头到尾过一遍，确认没有漏掉的再输出。

# 句子（按编号处理，输出也按这个编号）
{{sentences}}

# 输出格式
严格输出一个 JSON 对象，不要任何额外文字、不要 markdown 代码块围栏：

{
  "results": [
    {
      "index": 1,
      "words": [{"word": "...", "meaning": "中文"}],
      "collocations": [{"phrase": "...", "meaning": "中文", "note": "中文语境说明，简短，可空"}]
    }
  ]
}

results 数组长度必须等于句子数，index 从 1 开始按顺序对应。
```

**已知残留小瑕疵**（不影响"够用"结论，建流水线时留意）：
- 个别可分离短语动词保留了句中的代词（如 `put it off` 没简化成 `put off`），跟 `write off`
  这类处理不一致，属边界情况，靠 note 区分即可，不必强求统一。
- 偶尔会把语块里的核心名词也单独拆成一条生词（如 `take a toll on` 之外又给了 `toll` 一条），
  与 collocation 有轻微重复——真建 `words`/`collocations` 两表时这条重复入库风险要在去重逻辑
  里覆盖到（字面去重可能覆盖不了，因为一个是词一个是短语，字面不同）。
- 少数固定被动搭配（如 "be made redundant"）模型倾向于只拆出核心词（`redundant`）而不是整个
  搭配，边界判断上偏保守，实测影响不大。

### 5. 导出（Anki Export）— 对齐 langhelper

- 按语言分文件导出（语言不写进内容，由 langhelper 导入对话框选择）
- 病句 → JSON 数组；生词 → 纯文本每行一词；表达块（话术 / collocation）→ JSON 数组
- 详见"导出格式契约"

---

## 分工总表

| 活学能力                                   | 谁做                                                                         |
| ------------------------------------------ | ---------------------------------------------------------------------------- |
| 口语实时演练 + 应变                        | **Agent 独有**                                                               |
| 演练中埋钩子诱导旧表达（口语）             | **Agent 独有**（二期，✅ 已实现）                                            |
| 精听真实材料 + 收藏没听懂的句子            | Trancy 扩展（外包，双字幕/收藏/导 CSV）                                      |
| 分诊：语音 vs 语言（外层一刀）             | **你自己标**（只有你知道"认识但没听出来"）                                   |
| 分诊：生词 vs 语块（里层一刀）+ 提炼       | **Agent 用 Groq**（吃 Trancy CSV；提炼质量不够再局部升 Haiku）               |
| 语音现象留痕                               | **Agent 的 `phonetic_notes`**（只存不导，不刷不诱导）                        |
| 今日新学 collocation 口语激活              | **Agent 的诱导**（今日通道，方案 A）                                         |
| 萃取病句四要素 + 回推 zh 题面              | **Agent 的 Debrief**                                                         |
| 推荐专业应对话术（处理难缠客户）           | **Agent 的 Debrief**（存 `pro_phrases`，进口语诱导循环，并导出「表达块卡」） |
| 病句低阶还原练习                           | langhelper（离线零成本）                                                     |
| 病句高阶同类错误诱导                       | langhelper（sentence_high prompt）                                           |
| 生词认句 / 翻译诱导                        | langhelper（word_gen / word_translate prompt）                               |
| 话术 / collocation 文字侧诱导              | langhelper（表达块卡：情境应对召唤 / 搭配填空）                              |
| 抗遗忘硬刷                                 | Anki                                                                         |

---

## Debrief 输出 Schema（agent 内部存 SQLite）

每条病句记录：

```json
{
  "language": "fr", // 内部检索用（口语诱导），导出时剥离
  "zh": "...", // 中文原意/题面 —— Debrief 需回推学习者的表达意图
  "en_wrong": "...", // 学习者原句（通用槽位名，法语也用此字段，勿被 en 前缀误导）
  "en_correct": "...", // 地道修正
  "error_note": "...", // 思维错误分析（中文）
  "pattern": "...", // 错误类型标签
  "mastery": 0, // agent 自己的口语掌握度
  "last_practiced": "..." // ISO 时间，口语诱导检索用
}
```

每条生词记录：

```json
{
  "language": "fr",
  "word": "...", // 目标词（外语）
  "meaning": "...", // 中文词义（可空）
  "mastery": 0,
  "last_practiced": "..."
}
```

每条专业话术记录（新表 `pro_phrases`）：

```json
{
  "language": "fr",
  "scenario_type": "...", // 场景类别（诊所/面试…），专业话术常场景绑定；导出剥离
  "phrase": "...", // 话术本身（目标语）—— 导出的正面
  "meaning": "...", // 中文意思 —— 导出的背面
  "dimension": "...", // 专业维度：同理承接/设立边界/降级冲突/重定向解决/vouvoiement；导出剥离
  "usage_note": "...", // 何时用、为何专业（中文）—— 导出降级进 note
  "mastery": 0,
  "last_practiced": "..."
}
```

`phrase` / `meaning` / `usage_note` / `language` 字段用于**导出「表达块卡」**（见导出契约）。
`scenario_type` / `dimension` / `mastery` / `last_practiced` 是 agent 内部字段（诱导循环与检索用），
**导出时剥离**。

**Debrief 一次调用的完整输出**是 `{sentences: [...], words: [...], pro_phrases: [...]}`
三部分，分别落 `expressions` / `words` / `pro_phrases` 三张表。

**精听提炼（模块 4）的产出走另一条写入路径**，与 Debrief 并列、互不干扰：语块 → `collocations`
表，生词 → 复用 `words` 表，语音现象 → `phonetic_notes` 表（只存不导，见下）。每条 collocation 记录：

```json
{
  "language": "en",
  "phrase": "...", // 地道搭配/表达块（目标语）—— 导出「表达块卡」正面
  "meaning": "...", // 中文意思 —— 导出背面
  "note": "...", // 语境说明（有材料主题就带一句，日常搭配写日常用法，可空）—— 导出降级进 note
  "source": "mining", // 来源标记（区分 pro_phrases）；导出剥离
  "mastery": 0, // 口语掌握度（诱导循环用）
  "last_practiced": "..." // ISO 时间；今日通道靠 mastery=0 + 此字段为空/最早排前
}
```

`collocations` 与 `pro_phrases` 共用「表达块卡」导出格式（都映射到 `phrase`/`meaning`/`note` 三槽，
剥离各自的内部字段），但**是两张表**——区分留在上游（pro_phrases 有 dimension/scenario_type、走
话术诱导；collocation 无、走搭配诱导）。

**精听提炼的第三类产出——语音现象，走 `phonetic_notes` 表，不导出、不刷、不诱导。** 病根是听觉
解码能力（认识但没听出来），不是记忆缺口，故不接任何抗遗忘/诱导管道。每条记录：

```json
{
  "language": "en",
  "sentence": "...", // 没听出来时所在的整句（保留语境）
  "word_or_span": "...", // 你指的"哪里没听清"：句中哪个词/哪一小段（你能可靠指出）
  "source": "...", // 来源链接（句表 CSV 的 URL），便于回去重听那一段
  "date": "..." // 采集日期
}
```

**只记"哪里"、不记"原因"**：现象类型（连读/弱读/吞音）需要语音学判断，你自己也常分不清，硬填是
噪声非数据——留给未来真要练时由模型看 `sentence`+`word_or_span` 事后推断。这也贯彻"两刀切"：
你可靠判断的（哪里）你标，需要专业分析的（为什么）留给模型，别让你干模型该干的活。

`phonetic_notes` **只存不导**——不进任何 `{lang}_*.json/txt` 导出文件，不进 langhelper，不进
`induction.py`。它的用途是**攒数据 + 自我认知**：跑一段时间后这张表就是你个人的"听力盲区地图"
（不是"不认识的词"，是"认识但耳朵还原不出的声音模式"；靠 `word_or_span` 看你反复在哪些词/
哪类短语上栽，模式自会浮现，不必你贴语音学标签）。**一期只做识别 + 留痕，不建跟读/发音训练
机制**——因为语音现象占"没听懂"的比例尚未验证，为一个未验证频率的需求上机制违背"别过度工程"。
跑一阵看这表堆多少、反复是哪几种，再决定要不要为它建 shadowing/0.8x 跟读之类的训练（届时数据
驱动，可进可退）。**注意不要把它接进语音演练做实时发音反馈**：会撞"演练中绝不纠错"铁律，且
Debrief 现围绕"想表达却没表达好"（产出侧）设计，塞进感知侧发音复盘会失焦。

**关键点 `zh` 的来源：** 语音演练里学习者说的是外语（可能说错），没有现成中文原句。
Debrief 必须**回推学习者想表达的意思，写成中文 zh**，否则 langhelper 病句卡低阶（显示 zh 让你还原外语）没有题面。

---

## 导出格式契约（langhelper 导入器已验证）

三种卡型：病句卡（JSON）、生词卡（txt）、表达块卡（JSON）。三者都按语言分文件、
language 不写进内容、导出时剥离所有内部字段、纯净输出不带代码围栏。

### 病句卡 → JSON（parse_sentences 首选路径）

按语言分文件，如 `fr_sentences.json` / `en_sentences.json`。
每份是一个数组，元素**只含这五个字段**（剥离 language / mastery / last_practiced）：

```json
[
  {
    "zh": "中文题面",
    "en_wrong": "原句",
    "en_correct": "地道修正",
    "error_note": "思维错误分析",
    "pattern": "错误类型"
  }
]
```

约束：

- 每条至少要有 `zh` 或 `en_wrong` 才会被 langhelper 收录 —— 稳妥起见两者都给
- 不要带 ```json 代码块围栏（langhelper 能容忍，但干净为好）
- language 字段不写进 JSON（langhelper 只认那五个字段，多余字段被忽略）

### 生词卡 → 纯文本（parse_words）

按语言分文件，如 `fr_words.txt` / `en_words.txt`。
每行一个词，格式 `词|中文义`，义可省略：

```
resilient|有韧性的
vouvoiement|敬语（用 vous 称呼）
empathie
```

约束：

- langhelper 会自动大小写不敏感去重
- 不是 JSON，就是纯文本

#### 词表清洗规则（Trancy 词表 `Translation` → `words.meaning`）

Trancy 词表 CSV 的 `Translation` 字段偏脏，入库前清洗成简洁词义（一期就做，纯字符串处理、
无需模型）。字段结构固定：**按 `;` 分段，每段形如 `词性. 释义1 释义2 …`，末尾常带 `web.` 段
（网络释义，质量最差，含剧名等噪声）**。规则：

1. 按 `;` 切段，**丢弃以 `web.` 开头的段**（网络释义）。
2. 每个词典段（`n.`/`adj.`/`adv.`/`v.` 等）只保留**前 1–2 个主释义**（空格分隔的头一两个）。
3. 单词性时可省词性前缀（如 `独处 独居`）；多词性时保留前缀区分（如 `n. 过度 超过; adj. 超额的`）。
4. 去掉尾部/多余空格。

参考实现：

```python
import re

def clean_meaning(raw: str) -> str:
    segs = [s.strip() for s in raw.split(";") if s.strip()]
    segs = [s for s in segs if not s.lower().startswith("web.")]  # 丢 web 段
    parts = []
    for seg in segs:
        m = re.match(r"^([a-z]+\.)\s*(.+)$", seg)  # 分出词性前缀与释义
        if m:
            pos, defs = m.group(1), m.group(2).split()
            parts.append((pos, defs[:2]))          # 每词性取前 2 个主释义
        else:
            parts.append(("", seg.split()[:2]))
    if len(parts) == 1:                            # 单词性省前缀
        return " ".join(parts[0][1])
    return "; ".join((f"{pos} " if pos else "") + " ".join(defs) for pos, defs in parts)

# clean_meaning("n. 独处 独居; web. 孤独 寂寞 孤寂") -> "独处 独居"
# clean_meaning("n. 过度 超过 过分 过多的量; adj. 超额的 额外的; web. 过量") -> "n. 过度 超过; adj. 超额的 额外的"
```

`Phonetic`（音标）可一并存进 `words`（新增可空字段）备用，不参与导出（生词卡格式只有 `词|义`）。
"前 1–2 个主释义"的具体条数可按实际观感微调，规则一次写死即可。



按语言分文件，如 `fr_phrases.json` / `en_phrases.json`。
每份是一个数组，元素**只含这三个字段**（剥离 language / dimension / scenario_type /
mastery / last_practiced 等所有内部字段）：

```json
[
  {
    "phrase": "地道表达块本身（目标语）",
    "meaning": "中文意思",
    "note": "用法提示（中文，自由文本，可空）"
  }
]
```

**卡型语义**：装"一个地道的目标语表达块 + 中文意思 + 用法提示"。并列于病句卡（你说错了→怎么改）
和生词卡（这个词什么意思）之外的**第三种卡型**，专收**整块搭配 / 整句表达**——即病句卡的五字段
（需要 en_wrong）和生词卡的单词粒度都错配的那类对象。

**两类上游来源，共用此卡型（卡型共享，来源 schema 不共享）：**

- **专业应对话术**（agent `pro_phrases` 表）：`phrase` = 话术整句，`meaning` = 中文意思，
  `note` = 由 `usage_note` 降级而来的"何时用、为何专业"。`dimension`（同理承接/设立边界/
  降级冲突/重定向解决/vouvoiement）与 `scenario_type` **不进卡**，导出时剥离——它们只在
  agent 诱导循环里用。
- **别人的 collocation**（输入侧管线，非 agent 演练萃取）：`phrase` = 地道搭配，
  `meaning` = 中文意思，`note` = 语境说明（有 podcast/文章主题就带一句，是日常搭配就写日常
  用法，无则留空）。无 `dimension`、无 `scenario_type`、无口语 mastery。

场景/维度这类"有时有、有时没有"的信息**一律降级进自由文本 `note`**，不做结构化字段——
避免用一张 schema 硬套两种来源导致字段填不满。区分留在上游各自的表里（pro_phrases 要参与
agent 诱导、要按维度组织；collocation 不用），langhelper 导入器**不区分来源**，只看到一串
"表达块卡"。

**约束：**

- 每条至少要有 `phrase` 才会被 langhelper 收录 —— `phrase` 是卡片正面，不可空
- `meaning` 建议给（作背面）；`note` 可省略（自由文本槽，容纳"有时有场景、有时没有"的参差）
- language 字段不写进 JSON（langhelper 只认这三个字段，多余字段被忽略）
- 不要带 ```json 代码块围栏（沿用病句卡约束）
- langhelper 侧按 `phrase` 大小写不敏感去重（沿用生词卡去重惯例；话术与 collocation 若产出
  同一 `phrase`，字面去重即可，避免同块表达两条卡）

**诱导玩法（langhelper 侧，高阶）**：属"语境召唤"大类（优于翻译诱导、不适用造错句）。
因上游两类对象的最佳诱导略有分叉，做成同一诱导的两个变体，按 `note` 是否含场景信息切换：

- 含场景（多为话术）→ **情境应对召唤**：给使用场景/半句语境，诱导补出整句地道应对
- 无场景（多为 collocation）→ **搭配填空**：给句子挖空该搭配，诱导填出地道词组

玩法可迭代，先打通卡型与导出即可（参照 langhelper 现有 sentence_high / word_gen /
word_translate 分玩法先例）。

### 导入流程（人工步骤，记录备查）

1. Agent（或输入侧管线）生成分语言文件（`{lang}_sentences.json` / `{lang}_words.txt` /
   `{lang}_phrases.json`）
2. 在 Anki 里用 langhelper 的对应导入入口：
   - 病句 → "Import Chinglish sentences"
   - 生词 → "Batch add words"
   - 表达块 → "Import phrases"（待 langhelper 新增该入口）
3. 对话框里**选对应语言**（en / fr），粘贴对应文件内容
4. 选择/确认 deck，导入

---

## 技术栈

- **后端**：Python + FastAPI
- **语音编排**：Pipecat——把 STT/LLM/TTS 接成一条全双工实时 pipeline（VAD 断句、
  支持打断），transport 用 `SmallWebRTCTransport`（不依赖 Daily 云服务，适合本地单人场景）
- **LLM（角色扮演）**：Groq（当前用 `openai/gpt-oss-120b`，Groq 自己托管的开放权重模型，
  见 CLAUDE.md 实现踩坑记录）；**法语地道度务必持续关注**
  （vouvoiement、诊所同理心话术若撑不住，角色扮演层可换 Mistral/Claude/GPT，STT 仍留 Groq）
- **STT**：Groq Whisper（法语英语都强、快、便宜；Pipecat 的 `GroqSTTService` 是 VAD 分段，
  不是逐词流式，说完一段才转文字，符合 Whisper 本身的限制）
- **TTS**：Azure Speech（即 Edge-TTS 的正牌合规版，法语声音一致）
- **存储**：SQLite（agent 的唯一真相源）
- **前端**：最薄单页 —— 场景选择、Pipecat client（`@pipecat-ai/client-js` +
  `@pipecat-ai/small-webrtc-transport`）接管录音/放音、对话气泡、Debrief 卡片、
  导出面板、"结束"按钮

## 存储架构原则

- **Agent 的 SQLite 是唯一真相源**，Anki 是下游消费者。精听提炼（模块 4）是 agent 内部模块，
  **用同一个 SQLite**（新增 `collocations` 表、`phonetic_notes` 表），不另立独立库——因为目标是
  整合工作流而非分开的小软件，同进程内模块共享一库，没有跨进程锁竞争/故障传染。这跟"输入端
  独立时别合库"不矛盾：前提从"独立管线"变成"内部模块"，结论随之从"别合库"变成"本就一库"。
- **不直接读写 Anki 的 collection.anki2**（schema 不稳、有并发锁、数据散在卡片字段里）
- 数据流单向：agent SQLite →（提炼/Debrief 产出）→ 导出文件 → langhelper 导入 → Anki。
  上游 Trancy CSV 只读消费（采集工具外包），下游只写 langhelper 格式（Anki 外包），两头标准接口。
  `phonetic_notes` 是**只入不出**的旁支：入库后不参与任何下游导出。
- **掌握度一期不双向同步**：口语掌握度 agent 自己管，文字掌握度 Anki 自己管
  （两种能力本就不同，分开 track 反而合理）。真要同步，二期用 AnkiConnect，不碰底层文件
- **话术双轨提醒**：`pro_phrases` 现在有两个 mastery——口语侧（agent 诱导循环）与文字侧
  （导出成表达块卡后由 Anki 管）。沿用"一期不双向同步"，两者各管各的，别混。

---

## 难缠客户练习与配置分层

专业话术推荐（模块 2.5）要有素材，前提是**演练里角色真的会难缠**。难缠相关的参数按
**变化频率**分三层，别压成一个笼统 config：

- **本次难缠程度**（每场演练临场选）：温和 / 中等 / 难缠 / 极难缠。前端每次开练时选，
  通过 Pipecat `/start` 的 `body`（见 CLAUDE.md 踩坑记录：自定义字段必须套 `body`）传给 bot。
- **场景默认难缠程度**（每个场景一个默认）：存 `scenarios` 表，前端不选就用它。
- **难缠红线（满级上限）**：**写死为常量**，不做配置。它是产品铁律不是旋钮——
  即使选"极难缠"，角色也不能突破"仍可被专业方式化解"的边界（不无理取闹到对话崩溃、
  不人身攻击、始终留有专业应对的空间）。红线和旋钮是两回事：旋钮转到底也不能越过这堵墙。

### 配置项（`config.py`，可被 `.env` 覆盖，沿用 GROQ_MODEL 那套）

- `INDUCTION_MAX_TARGETS`：一次诱导注入的目标总数上限（现有 N 条的那个值）
- `INDUCTION_MIN_PHRASES`：其中**保底**来自 `pro_phrases` 的条数（若库里有未掌握的话术）。
  这保证专业话术不被病句/生词/collocation 淹没。
- **固定每场配比（取代阈值/限时窗口）**：四来源（病句 / 生词 / collocation / pro_phrases）在
  `INDUCTION_MAX_TARGETS` 内按固定配比采样，`pro_phrases` 保底优先。**不设"库涨到多少才加保护"
  的阈值**——配比一次写死，冷启动可跑（某来源没货时名额让别的顶上），collocation 涨到多少也只在
  自己名额内竞争、淹不到 pro_phrases。今日新学 collocation 走"今日通道"（高优先级、当天对话），
  不占常规配比名额（见模块 3）。

---

## 分期计划

**一期（使用优先，先跑通）**

- 文字版核心闭环：角色卡对话 → "结束" → Debrief → 存 SQLite → 导出 langhelper 格式
- 语音层：Pipecat 全双工实时语音（Groq STT/LLM + Azure TTS，VAD 自动断句、可打断）
- 演练/Debrief 双模式严格分离——全双工只变语音怎么传，纠错时机铁律不受影响

**二期**

- 口语侧诱导：新场景前检索旧表达注入角色卡隐藏目标 ✅ 已实现
- 掌握度更新算法：演练结束后复盘诱导目标有没有用上，更新 mastery ✅ 已实现
- 专业应对话术推荐（模块 2.5）：Debrief 产出 → `pro_phrases` 表 → 进口语诱导循环；
  配套"难缠程度"角色卡维度
- **表达块卡导出（新增）**：langhelper 新增第三种卡型「表达块卡」+ "Import phrases" 入口；
  接通 `pro_phrases` → `{lang}_phrases.json` 导出管道；collocation 复用同一卡型格式
- **精听提炼模块（模块 4，新增）**：吃 Trancy 两种 CSV（词表直入 `words`；句表走完整流水线）→
  句表以"没听懂的地方"为中心三类分诊（外层"语音 vs 语言"你自己标、标"哪里"不标"原因"；里层
  "生词 vs 语块"用 Groq 切）→ 语块/生词存 `collocations`/`words` 表并导出表达块卡/生词卡，语音
  现象存 `phonetic_notes`（只留痕不导）。采集外包 Trancy（导 CSV），只有提炼进 agent。配套
  "精听待处理"页（复用现有前端），批量为主、单条为辅。
  - **落地顺序：先验 Groq 提炼质量，再建流水线。✅ 已验证（2026-09-02）**。整条流水线里导入/
    遍历/入库都是确定能做成的 CRUD+界面，**唯一不确定的是 Groq 能否从整句可靠拎出正确语块**
    （直接决定 `collocations` 表和表达块卡质量）。用 10 句真实测试句（覆盖空句/陷阱短语/多语块/
    生词语块混合等场景，见"精听提炼分诊+提炼 Prompt"）跑了两轮真实 Groq API（`openai/gpt-oss-120b`）：
    第一轮验证两项核心风险——空句正确返回空、没把句子特有描述硬拎成语块，均通过；同时发现语块
    未还原词典原形（如 `curled up` 该是 `curl up`）、多语块句子偶尔漏一项两个小问题。针对性改
    prompt（加"词典原形"+"逐个检查再收尾"两条硬性要求）后第二轮全部修复。**结论：质量达到自用
    标准，维持全 Groq，不升级 Haiku**（残留两个小瑕疵：`put it off` 类保留代词、如 `take a toll on`
    与拆出的单字 `toll` 偶有重复入词，均可靠 `note` 字段/后续去重收尾，不影响整体判断）。验证用的
    prompt 已直接收进本文档（见下），建流水线时直接复用，不用重新设计。
- **collocation 口语激活**：今日通道（当天新学、mastery=0 优先注入当天对话）+ 历史通道（固定
  配比常规采样）。收进 agent 同库后，不再有跨库注入问题。
- 掌握度双向同步（若需要，AnkiConnect）
- 更多场景角色卡

## 非目标（明确不做）

- 不实现文字侧造句/翻译诱导（langhelper 已有）
- 不实现病句同类错误生成（langhelper sentence_high 已有）
- 不直接操作 Anki 数据库；抗遗忘外包 Anki，不自造
- **不自造播放器/取字幕/收藏**：双字幕/精听/取字幕/收藏外包给 Trancy（在线内容），本地无字幕
  素材退回 faster-whisper 命令行；agent 只吃导出的 CSV。收进 agent 的是**提炼**，不是采集工具——
  整合边界划在这条线上。
- **不做 collocation 的口语诱导"全覆盖"**：口语诱导是采样激活层，覆盖交给 Anki（见模块 3 采样激活
  原则）。故也不设"淹没保护阈值/限时窗口"，用固定每场配比一次写死。
- **一期不为语音现象建跟读/发音训练机制**：`phonetic_notes` 只识别 + 留痕。语音现象占"没听懂"的
  比例未验证，先攒数据再决定是否值得为它建 shadowing/0.8x 训练——可进可退，别提前上机制。
- **不把语音现象接进语音演练做发音反馈**：撞"演练中绝不纠错"铁律，且会让围绕产出侧设计的 Debrief
  失焦（感知侧发音 vs 产出侧表达是两回事）。

> 注：先前版本把"输入侧 collocation 采集"列为独立并行管线、非 agent 内建，并"一期不让 collocation
> 进口语诱导"。现已推翻——目标是整合工作流，提炼收进 agent 成模块 4、用同一 SQLite，collocation
> 进诱导循环（今日通道 + 固定配比历史通道）。此段保留以记录决策变更。

> 注（模块 4 输入与分诊，2026-09）：先前版本模块 4 输入接口写"SRT"、提炼用"Haiku"、三类由 LLM
> 统一分诊。现调整为——
> ① **输入 SRT → Trancy CSV**：Trancy 已跑通（在线双字幕/句级收藏/CSV 导出都验过），换独立播放器
> 不划算；CSV 比 SRT 更好解析；原契约本就写"接口可替换"，在预留弹性内。（本地无字幕素材退回
> faster-whisper 命令行。评估过 LLPlayer/Language Reactor/Migaku 等：LLPlayer 是本地 Whisper 生成
> 字幕的少数成品但 SRT 导出待验、且本地-only；Language Reactor/Migaku 的"分组收藏"能力普遍弱，
> 因这类工具设计上把分类交给下游 Anki。结论：不为"分组"换重工具。）
> ② **分诊改两刀切**：原"LLM 分三类"不成立——"认识但没听出来"（语音）的判断信息只在你听的当下、
> 模型从文本推不出，故**外层"语音 vs 语言"必须你自己标**，只有里层"生词 vs 语块"交模型。
> ③ **Haiku → Groq（复用现成客户端）**：分类是粗判断、Groq 主场且已搭好；仅"提炼"这步吃语言
> 细腻度，先用 Groq 试跑、不够再局部升 Haiku，看真实输出再定。
> ④ **新增第三类去向 `phonetic_notes`**：语音现象病根是听觉解码非记忆缺口，只留痕不导不刷不诱导；
> 一期不建跟读训练、不接演练发音反馈（撞纠错铁律）。
> ⑤ **输入拆词表/句表两条入口**（Trancy 词/句两级导两种 CSV）：词表（`Word/Phonetic/Translation/
> Date`）＝点词=生词，字段齐全、直入 `words`、不经 Groq；句表（`Sentence/Translation/URL/Date`）
> 走完整流水线。词表无 URL、句表有 URL（语音回溯靠它）。
> ⑥ **语音标记只记"哪里"不记"原因"**：`phonetic_notes` 去掉 `phenomenon`、改 `word_or_span`——
> 现象类型需语音学判断，使用者自陈常分不清，硬填是噪声；留给未来由模型事后推断。
> ⑦ **完整流水线一次做完**（导入去重→遍历三选一含「跳过」→Groq 分诊提炼→入三表），但**落地先验
> Groq 提炼质量再建界面**（唯一技术风险点）。此段保留以记录决策变更。